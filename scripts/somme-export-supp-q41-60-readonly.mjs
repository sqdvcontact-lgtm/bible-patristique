import{createHash}from'node:crypto';import{mkdirSync,readFileSync,writeFileSync}from'node:fs';import{createClient}from'@supabase/supabase-js';
const R='tmp/somme-liens-audit-2026-07-29',P=100,questions=Array.from({length:20},(_,i)=>`Question ${41+i}`);mkdirSync(R,{recursive:true});const env=Object.fromEntries(readFileSync('.env.local','utf8').split(/\r?\n/).map(x=>x.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/)).filter(Boolean).map(m=>[m[1],m[2].replace(/^["']|["']$/g,'')])),sb=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY),must=async(q,l)=>{const{data,error}=await q;if(error)throw Error(`${l}: ${error.message}`);return data};
const settings=await must(sb.from('parametres').select('cle,valeur,mis_a_jour').in('cle',['charte_ia','feedback_liens_protocole']),'parametres'),parameters=Object.fromEntries(settings.map(x=>[x.cle,{mis_a_jour:x.mis_a_jour,sha256:createHash('sha256').update(String(x.valeur??'')).digest('hex')} ])),segments=[],pagination=[];
for(const q of questions)for(let from=0;;from+=P){const page=await must(sb.from('segments').select('*').eq('id_oeuvre','A0013O0002').eq('ref_niv1','Supplément').eq('ref_niv2',q).order('segment_numero').range(from,from+P-1),`${q}:${from}`);segments.push(...page);pagination.push({objet:'segments',question:q,from,to:from+P-1,lignes:page.length});if(page.length<P)break}segments.sort((a,b)=>a.segment_numero-b.segment_numero);
const links=[];for(let off=0;off<segments.length;off+=P){const ids=segments.slice(off,off+P).map(s=>s.id);for(let from=0;;from+=P){const page=await must(sb.from('liens_bibliques').select('*').in('segment_id',ids).order('id').range(from,from+P-1),`liens:${off}:${from}`);links.push(...page);pagination.push({objet:'liens',segment_offset:off,from,to:from+P-1,lignes:page.length});if(page.length<P)break}}links.sort((a,b)=>a.id-b.id);
const cols='id_verset,ref,"num_TR0001","TR0001","num_TR0003","TR0003","num_TR0004","TR0004"',canonIds=[...new Set(links.map(x=>x.canon_id).filter(Boolean))],witnesses=[];for(let i=0;i<canonIds.length;i+=P)witnesses.push(...await must(sb.from('versets_lecture').select(cols).in('id_verset',canonIds.slice(i,i+P)),`temoins:${i}`));const specialIds=[...new Set(links.map(x=>x.verset_v2_id).filter(Boolean))],special_v2=[];for(let i=0;i<specialIds.length;i+=P)special_v2.push(...await must(sb.from('versets_v2').select('*').in('id',specialIds.slice(i,i+P)),`v2:${i}`));
const raw={exported_at:new Date().toISOString(),questions,parameters,pagination,segments,links,witnesses,special_v2};writeFileSync(`${R}/supp-q41-60-raw.json`,JSON.stringify(raw,null,2)+'\n');console.log(JSON.stringify({segments:segments.length,range:[segments[0]?.segment_numero,segments.at(-1)?.segment_numero],links:links.length,witnesses:witnesses.length,special_v2:special_v2.length,marked_local:segments.filter(s=>s.liens_revus_le||s.liens_revus_par).length,by_question:Object.fromEntries(questions.map(q=>[q,segments.filter(s=>s.ref_niv2===q).length])),types:Object.fromEntries([1,2,3,4].map(t=>[t,links.filter(x=>x.type===t).length])),special_targets:links.filter(x=>x.verset_v2_id||x.livre||x.chapitre).length,pagination,parameters},null,2));
