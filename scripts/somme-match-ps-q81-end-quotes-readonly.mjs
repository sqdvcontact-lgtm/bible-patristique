import { readFileSync, writeFileSync } from 'node:fs';
import { createClient } from '@supabase/supabase-js';
const ROOT='tmp/somme-liens-audit-2026-07-29',raw=JSON.parse(readFileSync(`${ROOT}/ps-q81-end-raw.json`,'utf8'));
const env=Object.fromEntries(readFileSync('.env.local','utf8').split(/\r?\n/).map(x=>x.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/)).filter(Boolean).map(m=>[m[1],m[2].replace(/^["']|["']$/g,'')])),sb=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY);
const STOP=new Set('alors aussi avec cette comme dans depuis elle elles encore entre est fait leur leurs mais meme nous pour quand sans selon sera sont sous tout tous toute tres une vous votre ainsi avoir celui cette dont etre faut plus'.split(' '));
const words=s=>(s??'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9\s]/g,' ').split(/\s+/).filter(w=>w.length>3&&!STOP.has(w));
const verses=[];for(let from=0;;from+=1000){const{data,error}=await sb.from('versets_lecture').select('id_verset,ref,TR0001,TR0003,TR0004').order('id_verset').range(from,from+999);if(error)throw error;verses.push(...data);if(data.length<1000)break}
const indexed=verses.map(verse=>({verse,sets:[verse.TR0001,verse.TR0003,verse.TR0004].filter(Boolean).map(t=>new Set(words(t)))})),linked=new Map;for(const l of raw.links){if(!linked.has(l.segment_id))linked.set(l.segment_id,new Set);if(l.canon_id)linked.get(l.segment_id).add(l.canon_id)}
const rows=[];for(const s of raw.segments){const quotes=[...(s.segment_texte??'').matchAll(/[«](.{12,400}?)[»]/gs)].map(m=>m[1]).filter(q=>words(q).length>=3);for(const quote of quotes){const q=new Set(words(quote)),scores=[];for(const item of indexed){let best=0;for(const set of item.sets){const score=[...q].filter(w=>set.has(w)).length/Math.max(1,q.size);if(score>best)best=score}if(best>=.35)scores.push({id_verset:item.verse.id_verset,ref:item.verse.ref,score:Number(best.toFixed(3))})}scores.sort((a,b)=>b.score-a.score);rows.push({segment_id:s.id,segment_numero:s.segment_numero,question:s.ref_niv2,quote,linked:[...(linked.get(s.id)??[])],top:scores.slice(0,5)})}}
writeFileSync(`${ROOT}/ps-q81-end-quote-matches.json`,`${JSON.stringify(rows,null,2)}\n`);console.log(JSON.stringify({verses:verses.length,quotes:rows.length,high:rows.filter(x=>x.top[0]?.score>=.65).length,high_unlinked:rows.filter(x=>x.top[0]?.score>=.65&&!x.linked.includes(x.top[0].id_verset)).length},null,2));
