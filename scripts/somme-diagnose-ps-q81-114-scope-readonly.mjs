import{readFileSync}from'node:fs';import{createClient}from'@supabase/supabase-js';
const env=Object.fromEntries(readFileSync('.env.local','utf8').split(/\r?\n/).map(x=>x.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/)).filter(Boolean).map(m=>[m[1],m[2].replace(/^["']|["']$/g,'')])),db=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY),questions=Array.from({length:34},(_,i)=>`Question ${81+i}`),{data,error}=await db.from('segments').select('id,segment_numero,ref_niv1,ref_niv2,ref_niv3,segment_texte').eq('id_oeuvre','A0013O0002').eq('ref_niv1','Prima Secundae').gte('segment_numero',10369).lte('segment_numero',12407).order('segment_numero');if(error)throw error;const outside=data.filter(s=>!questions.includes(s.ref_niv2));console.log(JSON.stringify({range_total:data.length,question_scope:data.length-outside.length,outside},null,2));
