import { mkdirSync, readFileSync, writeFileSync } from 'node:fs';
import { createClient } from '@supabase/supabase-js';
const ROOT='tmp/somme-liens-audit-2026-07-29';mkdirSync(ROOT,{recursive:true});
const env=Object.fromEntries(readFileSync('.env.local','utf8').split(/\r?\n/).map(x=>x.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/)).filter(Boolean).map(m=>[m[1],m[2].replace(/^["']|["']$/g,'')]));
const sb=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY);const must=async(q,l)=>{const{data,error}=await q;if(error)throw Error(`${l}: ${error.message}`);return data};
const questions=Array.from({length:6},(_,i)=>`Question ${76+i}`),segments=[];for(const q of questions)segments.push(...await must(sb.from('segments').select('*').eq('id_oeuvre','A0013O0002').eq('ref_niv1','Secunda Secundae').eq('ref_niv2',q).order('segment_numero'),q));segments.sort((a,b)=>a.segment_numero-b.segment_numero);
const links=[];for(let i=0;i<segments.length;i+=100)links.push(...await must(sb.from('liens_bibliques').select('*').in('segment_id',segments.slice(i,i+100).map(s=>s.id)).order('id'),`liens ${i}`));links.sort((a,b)=>a.id-b.id);
const ids=[...new Set(links.map(l=>l.canon_id).filter(Boolean))],witnesses=[];for(let i=0;i<ids.length;i+=100)witnesses.push(...await must(sb.from('versets_lecture').select('id_verset,ref,"num_TR0001","TR0001","num_TR0003","TR0003","num_TR0004","TR0004"').in('id_verset',ids.slice(i,i+100)),`témoins ${i}`));
writeFileSync(`${ROOT}/ss-q76-81-raw.json`,JSON.stringify({exported_at:new Date().toISOString(),questions,segments,links,witnesses},null,2)+'\n');console.log(JSON.stringify({segments:segments.length,range:[segments[0]?.segment_numero,segments.at(-1)?.segment_numero],links:links.length,witnesses:witnesses.length,marked:segments.filter(s=>s.liens_revus_le||s.liens_revus_par).length,by_question:Object.fromEntries(questions.map(q=>[q,segments.filter(s=>s.ref_niv2===q).length]))},null,2));
const candidateIds=['JAS.3.10','2KI.2.24','DEU.28.17','2SA.1.21','JUD.1.9','DEU.25.14','DEU.25.16','MAT.21.12','SIR.26.29','DEU.23.20','DEU.23.21','DEU.28.12','ISA.33.15','MAT.7.19','PSA.115.12','PRO.3.6','SIR.43.30','LUK.1.75','ROM.8.39'];
const candidateWitnesses=await must(sb.from('versets_lecture').select('id_verset,ref,"num_TR0001","TR0001","num_TR0003","TR0003","num_TR0004","TR0004"').in('id_verset',candidateIds),'témoins candidats');
writeFileSync(`${ROOT}/ss-q76-81-candidate-witnesses.json`,JSON.stringify(candidateWitnesses,null,2)+'\n');
