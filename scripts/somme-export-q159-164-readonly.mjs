import{mkdirSync,readFileSync,writeFileSync}from'node:fs';import{createClient}from'@supabase/supabase-js';
const R='tmp/somme-liens-audit-2026-07-29',P=100;mkdirSync(R,{recursive:true});const env=Object.fromEntries(readFileSync('.env.local','utf8').split(/\r?\n/).map(x=>x.match(/^\s*([A-Z0-9_]+)\s*=\s*(.*)\s*$/)).filter(Boolean).map(m=>[m[1],m[2].replace(/^["']|["']$/g,'')])),sb=createClient(env.NEXT_PUBLIC_SUPABASE_URL,env.SUPABASE_SERVICE_ROLE_KEY),must=async(q,l)=>{const{data,error}=await q;if(error)throw Error(`${l}: ${error.message}`);return data},questions=Array.from({length:6},(_,i)=>`Question ${159+i}`),segments=[],pagination=[];
for(const q of questions)for(let from=0;;from+=P){const page=await must(sb.from('segments').select('*').eq('id_oeuvre','A0013O0002').eq('ref_niv1','Secunda Secundae').eq('ref_niv2',q).order('segment_numero').range(from,from+P-1),`${q} ${from}`);segments.push(...page);pagination.push({objet:'segments',question:q,from,to:from+P-1,lignes:page.length});if(page.length<P)break}segments.sort((a,b)=>a.segment_numero-b.segment_numero);
const links=[];for(let from=0;from<segments.length;from+=P){const page=await must(sb.from('liens_bibliques').select('*').in('segment_id',segments.slice(from,from+P).map(s=>s.id)).order('id'),`liens ${from}`);links.push(...page);pagination.push({objet:'liens_bibliques',segments_from:from,segments_to:Math.min(from+P-1,segments.length-1),lignes:page.length})}links.sort((a,b)=>a.id-b.id);
const cols='id_verset,ref,"num_TR0001","TR0001","num_TR0003","TR0003","num_TR0004","TR0004"',ids=[...new Set(links.map(l=>l.canon_id).filter(Boolean))],witnesses=[];for(let from=0;from<ids.length;from+=P)witnesses.push(...await must(sb.from('versets_lecture').select(cols).in('id_verset',ids.slice(from,from+P)),`témoins ${from}`));
const candidateIds=['1CO.12.31','1PE.2.13','1PE.3.4','2TI.2.25','MAT.6.20','LUK.12.48','SIR.6.33','SIR.10.12','SIR.10.13','SIR.19.26','PSA.118.51','ISA.64.5','GEN.1.27','GEN.2.17','GEN.3.5','GEN.3.6','GEN.3.7','GEN.3.16','GEN.3.17','GEN.3.18','GEN.3.19','GEN.3.21','GEN.3.22','GEN.3.23','GEN.3.24','GEN.5.5','1CO.11.3','WIS.11.20'];const candidateWitnesses=await must(sb.from('versets_lecture').select(cols).in('id_verset',candidateIds).order('id_verset'),'témoins candidats');writeFileSync(`${R}/ss-q159-164-candidate-witnesses.json`,JSON.stringify(candidateWitnesses,null,2)+'\n');
writeFileSync(`${R}/ss-q159-164-raw.json`,JSON.stringify({exported_at:new Date().toISOString(),questions,pagination,segments,links,witnesses},null,2)+'\n');console.log(JSON.stringify({segments:segments.length,range:[segments[0]?.segment_numero,segments.at(-1)?.segment_numero],links:links.length,witnesses:witnesses.length,candidate_witnesses:candidateWitnesses.length,marked:segments.filter(s=>s.liens_revus_le||s.liens_revus_par).length,by_question:Object.fromEntries(questions.map(q=>[q,segments.filter(s=>s.ref_niv2===q).length])),pagination},null,2));
